<h1>Articles</h1>

<ul class="list-group">
  <% @articles.each do |article| %>
    <li class="list-group-item">
      <%= link_to(image_tag(article.thumbnail), article_path(article)) %>
    </li>
  <% end %>
</ul>
<br>

<%= link_to "New Article", new_article_path, class: "btn btn-primary" %>
