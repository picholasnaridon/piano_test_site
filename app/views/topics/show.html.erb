<h1><%= @topic.name %> Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path %>
    </li>
  <% end %>
</ul>


<%= link_to 'New', new_topic_article_path(@topic, @article) %>
